interface:
  display_name: "WB Card Chrome Audit"
  short_description: "Safe visible Chrome audit workflow for one Wildberries product card."
  default_prompt: "Use $wb-card-chrome-audit to collect visible text data from one Wildberries product card through installed Google Chrome with split preflight, screenshot OCR gates, and no media downloads."
